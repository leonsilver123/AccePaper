# HANDOFF — 2026-09-09 16:55 (无人值守科研论文生产系统 DSH 插件)

> 给下一个会话/主 Agent 的交接文档。任何新会话先读本文件 + 下方权威寄存器，再动手。

## 0. 一句话状态
系统在向"论文级产物链"推进中；**已过双审计(#13 限定口径可关、T19=离线 Direct 闭环+部分 AgentLoop Runtime-verified)、E/C/F 三轨已入库**；当前主战场是 **T30 本地插件加载(轨道 A)**——Attempt 1/2 确定性失败已记录，Attempt 3 已把 6 个 research 包以纯 npm(无 pnpm/无 Junction)链式装入临时 profile 且 core/tools 可独立 require，**下一步是真 DSH_HOME profile boot 1120 验证 host peer 经 heal-fallback 解析**。

## 1. 用户最新裁定与总目标(摘要,勿偏离)
- 最终验收=用户能投出去的论文;通用链路优先(交通+计算机双域 L0=中科院分区∪CCF);发布形态=插件(开源合规)。
- 执行纪律(夜跑版): 不接真实网关/密钥;不 push/发布/tag/新备份;不关 B-PICK;不优先 VSDX;不改无关宿主 API;子 Agent 不 commit;package.json/lock/tsconfig/Bundle/profile 仅主 Agent 改;每里程碑原子 commit+工作树干净;疑问走 decision-needed.md + fail-closed;完成度禁止以测试数代替,必须区分 Designed/Implemented/Unit-verified/Integration-verified/Runtime-verified/Clean-install verified/Audit-passed/Blocked。
- #13 关闭口径(用户钦定): 七工具正式注册=Audit-passed;三项 model_ready=AgentLoop Runtime-verified;四项 pipeline_only=Direct Fixture only;七工具全 ToolRuntime Runtime-verified=未实现;ARCH-BLOCK-01 保留。
- T30 不得轻易判 BLOCKED: 需 3 条本地路径均有源码+运行失败证据;ERR_PNPM_FETCH_404 只证 npm-fetch 失败。
- 停止门槛(用户设定): 至少 1 条 T30 本地路径或三路失败证据 + Case Pack Happy/Recovery 产物 + Session 重启恢复 + ResearchView 组件 DOM + (1120 成功则真实浏览器 DOM) + 新双审计 P0/P1 清零。

## 2. Git 拓扑与近期提交(真实)
工作仓库=**G:\AccePaper-main\deepseek-harness-dev**(唯一写入仓;master)。
线性近链(旧→新): …→ `b73a652`(research-app bundle)→ `16ab7f0`(七工具目录)→ `4df2262`(Phase1 注册)→ `d4de4b2`(P1.1 硬化)→ `8ea8eac` → `5b47c25`(授权模型定稿+事务性)→ `51e3aa1`(严格 Schema)→ `1eb3d71`(A+B 轨道)→ `7abab32`(Invoker 接口+Direct+repro-check)→ `d3c3715`(AgentLoop 闭环+guard 作用域修复)→ `64bd81f` → `b075435`(AgentLoopResearchToolInvoker)→ `56472e5`(R2 语义)→ `80d7eb6`(R1 种子)→ `735b1a3`(R1 完成)→ `aeb6af2`+`1d859bf`(R3 通道统计)→ `120db48`(R3 planned/actual 修正)→ `01e94e5`(S1 返工批次)→ `14e19d7`(S1-P2-5)→ `8cbeec6`+`77699ef`(F1 返工)→ `b324b93`(轨道 E SnapshotStore)→ `6b20940`(轨道 C Case Pack)→ `204f9ea`(轨道 F DOM)。
当前 HEAD ≈ `204f9ea`;树基本干净(有未跟踪: repo `plan/overnight/2026-09-09/` 下 t30 实验产物与 case-pack 已提交子集)。

## 3. 完成度矩阵(如实,勿虚标)
| 项 | 状态 |
|---|---|
| 七工具严格 Schema(input-schema.v1) | Implemented+Unit-verified;TOCTOU 快照修复(审计闭环) |
| 注册事务性/生命周期/readiness identity/capability 句柄授权 | Implemented+Unit-verified;S1 PASS |
| loader 兼容层(错误码/无泄漏/版本门) | Implemented+Unit-verified;S1 PASS |
| AgentLoop model_ready(真实 ToolRuntime 全链) | **Runtime-verified**(closed-loop + AgentLoopResearchToolInvoker 双证据) |
| AgentLoopResearchToolInvoker / DirectResearchToolInvoker | Implemented+Unit(+Runtime for model_ready) |
| pipeline_only 经 ToolRuntime | ARCH-BLOCK-01(未实现,不改宿主) |
| T19 语义校准(SEMANTIC_TASKS/R1 执行器全 Invoker/R3 actual 通道) | Implemented+Unit-verified;T19=**Offline Integration-verified**(非完整 Runtime) |
| 双审计(S1 安全/F1 功能语义) | **Audit-passed(P0/P1 清零,复审 CLOSED)**;接受清单与理由在 audit-register.md |
| 轨道 E SnapshotStore | Implemented+Unit-verified(8/8;commit b324b93) |
| 轨道 C 交通 Case Pack v1(7 JSON) | 数据已入库(commit 6b20940);**尚未跑出 Happy/Recovery run 产物** |
| 轨道 F ResearchView 组件 DOM | **Integration-verified**(10/10,~46 DOM 断言;commit 204f9ea) |
| T30-R/1120 真启动 | **进行中(轨道 A)**:Attempt1 pnpm+file:(workspace:^ 拒,可复现);Attempt2 npm pack tgz(EUNSUPPORTED workspace:,可复现);Attempt3 重写+纯 npm 链装 6 包→ core/tools require OK;待 boot 验证 host peer heal-fallback |
| Clean-install verified | CONDITIONAL(未被正式 clean install 证实;pnpm shim 损坏) |
| 轨道 B tarball 依赖闭包 | 未单列;核心已在 Attempt3 实测(pack→rewrite→install→require) |
| 轨道 D Case Pack runner | 未实现(待 C schema + runner 设计) |
| Session 重启恢复(进程级用例) | 未实现(SnapshotStore 底层已备) |
| 真实浏览器 DOM / 1120 | 依赖 T30 boot |
| Coverage/CI 一键验收 | 未做(最后) |

## 4. 权威文件(新会话必读)
- repo 内(可跟踪/已提交区): `plan/overnight/2026-09-09/case-pack-traffic/*`(C 数据);`packages/research/dsh-research-core/src/snapshot-store.ts`(E);`packages/research/dsh-research-web/tests/researchview-component.dom.spec.ts`(F)。
- repo 内(实验/未提交): `plan/overnight/2026-09-09/t30-attempts.md`(轨道 A 三 Attempt 证据+命令)、`t30-tmp/`、`$TEMP/t30-*`(g-9/主线程临时环境,可复算或清理)。
- repo 外 `G:/AccePaper-main/plan/overnight/2026-09-09/`: `execution-ledger.md`、`decision-needed.md`(D1/D2/D3/ARCH-BLOCK-01)、`audit-register.md`(S1/F1 结果与处置)、`t19-s/semantic-step-tool-matrix.md`、`t19-s/toolruntime-lifecycle-observability.md`、`t19-s/a1-agentloop-recon.md`、`t19-s/t13r-phase11r-design.md`、`morning-report.md`(待本轮达标后产出)。
- 工作记忆: `G:/AccePaper-main/.workbuddy/memory/2026-09-09.md`。

## 5. 环境事实与坑(重要)
- Windows + Git Bash;npm 10.9.7 可用;**pnpm PATH shim 损坏**(corepack);`dsh plugin` 硬 spawn `pnpm`(PATH 查找)→ 本地路径可行前提=可用 pnpm 实体或绕开。
- npm registry 可达(出现过 404/网络);npm pack/npm install 可用;Windows 路径须 C:\ 盘符形式,`/g/...` 会 ENOENT。
- npm 目录依赖=符号链接,`file:../` 按 realpath 解析失败 → **必须 tgz(复制)安装**。
- research 包未发布(workspace:^);boot: bundle 名解析 INSTALL_ANCHOR(apps/cli)→profile node_modules;healProfilesModuleFallback 共享闭包含多数 host(含 research-web)。
- 子 Agent 配额 429: **重置 2026-09-10 00:35:36 UTC+8**;期间子 Agent 不可靠;主线程工具不受影响。曾有先例: 声明的重置点之后可正常 spawn。
- Junction 仅诊断,不作正式证据。两个 plan 目录(仓外 G:/AccePaper-main/plan vs 仓内 repo/plan),别写混。
- research 全量测试: `./node_modules/.bin/vitest run packages/research`(当前 ≈990 用例全绿基准);tsc/lint: tsc -p 各包 --noEmit、oxlint(90)尽力 0、repo 门禁 49 规则 precommit。

## 6. 下一步最短路径(优先级序)
1. **轨道 A**: 建临时真实 DSH_HOME profile(web 模板,DSH_HOME 指向 $TEMP)→ 把 packs3 重写的 6 包 seed 进 profile node_modules → `dsh.profile.bundles` 加 research-app → `dsh web` boot(期望 1120)→ 记录 host peer 是否经 heal-fallback 解析;失败则 Attempt 4(把缺失 host peer 从 apps/cli 闭包以 tgz/复制 seed 进 profile)或三条全败后按证据判 BLOCKED。
2. 成功后 T30-R 验收: roster/七工具 readiness/Mock Session/16 步 run/start-stop-restart/端口冲突。
3. **轨道 D**: 按 case-pack v1 实现 runner(读 pack→建 run→驱动现有 T19 Invoker→逐步 actualChannel→Happy 到 E2 pending;Recovery 含 not-found/abstained/rollback);字节级可复现(白名单非确定字段)。
4. **Session 进程级重启**: SnapshotStore+drive 半程 save→restart→restore,断言不重跑/重放拒/损坏 fail-closed。
5. 真实浏览器 DOM(1120 成功后): Research tab/ResearchView/九面板/状态/Case Pack 一致/E2 pending/Fixture 徽章/console 干净。
6. 新双审计: Runtime Audit + Scientific Truthfulness Audit(read-only Agent;P0/P1 返工+原 Agent 复审)。
7. Coverage/CI 一键验收(最后): 行为分支优先,不降阈值/不大范围 ignore;纯类型/barrel 单独登记。
8. morning-report.md 在达标后产出(含 commit 拓扑/层级/测试/Runtime/DOM 证据/Case Pack/Direct-AgentLoop-Fixture 计数/审计/P0-P3/疑问降级/Mock 清单)。

## 7. 交接注意事项
- 主线程可继续一切;B/C/D/E/F 相关代码产出需主 Agent 验证后**单写入者原子提交**。
- 子 Agent 重派窗口: 2026-09-10 00:35:36 之后;之前避免无谓 spawn(429)。
- 更新权威寄存器(execution-ledger/audit-register/decision-needed/t30-attempts)与 .workbuddy/memory 后再收尾。
- 所有"完成"声明须能落到上述矩阵标尺与证据文件;不许把 Fixture 当真实、不许伪造 DOI/执行 id/产物。
